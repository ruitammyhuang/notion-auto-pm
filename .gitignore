# Notion token and sensitive config — NEVER commit these
notion_sync_config.json

# Auto-generated mapping files — rebuilt by Sync Now, not needed in version control
notion_sync_mappings.json
notion_sessions_mappings.json

# Sync log files (timestamped, generated on errors)
sync_log_*.txt

# Python
__pycache__/
*.pyc
*.pyo
.env

# macOS
.DS_Store
